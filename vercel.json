{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build:backend && cd smartshop-frontend && npm run build",
  "outputDirectory": "smartshop-frontend/dist",
  "installCommand": "npm install --legacy-peer-deps && cd backend && npm install --legacy-peer-deps && cd ../smartshop-frontend && npm install --legacy-peer-deps && npm install vite@5.4.2 --save-dev",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
